
export interface WebbingApplication {
  label: string;
  promptValue: string;
}

export interface MockupStyle {
  label: string;
  promptValue: string;
}

export interface GeneratedImage {
  id: string;
  base64: string;
}
